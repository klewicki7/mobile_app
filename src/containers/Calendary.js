import {connect} from 'react-redux';
import actions from "../redux/actions";
import InitialScreen from '../screens/CalendaryScreen';

const mapStateToProps = (state) => ({
    
});

const mapDispatchToProps = dispatch => ({
    
});

export default connect(
mapStateToProps,
mapDispatchToProps
)(InitialScreen);
